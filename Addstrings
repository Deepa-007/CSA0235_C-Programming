#include<stdio.h>
#include<string.h>
int main(){
char a[]="";
char b[]="";
int len1=strlen(a);
int len2=strlen(b);
char c[len1+len2];
for(int i=0;i<(len1+len2);i++){
    if(i<len1)
        c[i]=a[i];
    else
        c[i]=b[i-len1];
}
c[len1+len2]='\0';
printf("%s",c);
}
