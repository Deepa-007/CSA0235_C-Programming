#include <stdio.h>
int main()
{   int N,sum=0;
   float average;
   scanf("%d",&N);
   int a[N];
   for(int i=0;i<N;i++)
   {
       scanf("%d",&a[i]);  
       sum=sum+a[i]; 
   }
   printf("Sum is:%d",sum);
   average=(float)sum/N;
   printf("\n Average:%f",average);
    return 0;
}
